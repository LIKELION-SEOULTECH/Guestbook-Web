import React from "react";

function Header() {
    return (
        <header className="header">
            <h1 className="header-title">STLOG</h1>
        </header>
    );
}

export default React.memo(Header);
